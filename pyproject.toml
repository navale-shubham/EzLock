[project]
name = "ezlock"
version = "1.0.0"
description = "Password-based folder encryption using AES-256-GCM + PBKDF2"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "click>=8.4.1",
    "cryptography>=48.0.0",
]
