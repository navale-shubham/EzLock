[project]
name = "ezlock"
version = "2.0.0"
description = "A fast and easy to use encryption utility."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cryptography>=50.0.0",
    "pytest>=9.1.1",
    "typer>=0.27.1",
]
